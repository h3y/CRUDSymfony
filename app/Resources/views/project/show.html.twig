{% extends 'base.html.twig' %}

{% block body %}
    <h1>Project</h1>

    <table>
        <tbody>
            <tr>
                <th>Idpr</th>
                <td>{{ project.idPr }}</td>
            </tr>
            <tr>
                <th>Iduser</th>
                <td>{{ project.idUser }}</td>
            </tr>
            <tr>
                <th>Idis</th>
                <td>{{ project.idIs }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('project_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('project_edit', { 'id': project.idPr }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
